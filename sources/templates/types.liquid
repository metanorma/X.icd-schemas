
=== Types

{% assign type_defs = context.types["$defs"] %}

==== Type definitions

[cols="4",options="header,unnumbered"]
|===
| Name | Base Type | Description | Pattern/Enum

{% for type in type_defs%}

{%- assign def_name = type[0] -%}
{%- assign base_type = type[1].type -%}
{%- assign type_description = type[1].description -%}
{%- assign pattern = type[1].pattern -%}

| [[types-{{def_name}}]]{{ def_name }}
| {{ base_type }}
| {{ type_description | html2adoc }}
| {% if pattern -%}`++{{ pattern | replace: "|", "\|" }}++`{%- endif %}

{% endfor %}

|===